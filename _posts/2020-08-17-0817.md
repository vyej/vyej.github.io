---
layout: post
category: [notes]
title: conda的一點使用備忘
date: 2020-08-17 01:08:16
tags: [python, conda]
math: true
---

再次windows下使用conda。安裝好像也還好，沒有什麼。

備忘一下：

- 創建複製刪除環境
  
	  conda env list	
	  conda create --name myenv
	  conda create --name myenv python=3.8
	  conda create --name myclone --clone myenv
	  conda env remove --name myenv

- 安裝、更新
	  
	  conda list
	  conda install XXX
	  conda update  XXX
	  conda upgrade XXX
	  conda remove  XXX
		

- 打包環境

  1. YAML文件迁移
  
	  conda env export -n 原环境名称 > environment.yml
	  sed -i 's/name: 原环境名称/name: 新环境名称/g' environment.yml
	  conda env create -f environment.yml
	  
  2. conda-pack打包（离线环境最佳实践）
  
	  # 安装打包工具
	  conda install -c conda-forge conda-pack
	  conda pack -n 原环境名称 -o 原环境名称.tar.gz
	  # 创建目标目录
	  mkdir -p ~/miniconda3/envs/新环境名称
	  # 解压到新环境目录
	  tar -xzf 原环境名称.tar.gz -C ~/miniconda3/envs/新环境名称
	  conda activate 新环境名称
	  
--------




